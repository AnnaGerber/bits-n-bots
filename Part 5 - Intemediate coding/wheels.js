var five = require("johnny-five"),board, leftServo, rightServo;board = new five.Board();board.on("ready", function() {  leftServo = new five.Servo({ pin:  9, type: 'continuous' });  rightServo = new five.Servo({ pin:  10, type: 'continuous' });  // make sure both are stopped  leftServo.to(90);  rightServo.to(90);  board.repl.inject({    l: leftServo,    r: rightServo  });});